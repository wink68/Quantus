import { BrowserRouter, Route, Routes } from 'react-router-dom';

function Router() {
  return <BrowserRouter>
    <Routes>
      <Route></Route>
    </Routes>
  </BrowserRouter>
}

export default Router;